'use strict';

/* task #1 */
let admin
const name = 'John';

admin = name;

alert(admin);

/* task #2 */
const planet = 'Earth';
const firstName = null;

console.log(planet);
console.log(firstName);

/* task #3 */
const someVar = '10' + 10;
console.log(typeof someVar);
/*если один из операндов операции + является строкой, то второй операнд будет автоматически преобразован в строку*/
